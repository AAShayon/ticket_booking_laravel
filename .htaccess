<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Handle requests to the root directory
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L,QSA]
    
    # Handle direct access to public directory
    RewriteCond %{REQUEST_URI} ^/public/
    RewriteRule ^public/(.*)$ public/$1 [L,QSA]
</IfModule>